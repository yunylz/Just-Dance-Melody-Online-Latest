[package]
name = "JDMO"
version = "0.1.0"
description = "The personalized experience of JDMO for you!"
authors = ["JDMO Team", "RyuAtelier"]
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lib]
# The `_lib` suffix may seem redundant but it is necessary
# to make the lib name unique and wouldn't conflict with the bin name.
# This seems to be only an issue on Windows, see https://github.com/rust-lang/cargo/issues/8519
name = "tauri_app_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-opener = "2"
tauri-plugin-deep-link = "2"
tauri-plugin-dialog = "2"
tauri-plugin-fs = "2"
tauri-plugin-shell = "2"
tauri-plugin-http = "2"
tauri-plugin-updater = "2"
tauri-plugin-process = "2"
sysinfo = "0.32"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[target."cfg(any(target_os = \"macos\", windows, target_os = \"linux\"))".dependencies]
tauri-plugin-single-instance = { version = "2", features = ["deep-link"] }


# Read the optimization guideline for more details: https://tauri.app/concept/size/#cargo-configuration
# NOTE: lto/codegen-units are relaxed here to keep CI builds fast. The shipped
# binary is a thin webview shell (frontend is served remotely), so size is not
# critical.
[profile.release]
codegen-units = 16
lto = "thin"
opt-level = 2
panic = "abort"
strip = true
